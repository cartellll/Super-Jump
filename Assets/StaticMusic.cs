using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class StaticMusic
{
    // Start is called before the first frame update
    public static bool onMusic = false;
}
